{
  "name": "hello",
  "version": "1.0.0",
  "description": "simple game",
  "main": "main.js",
  "watch": {
    "build-dev": {
      "patterns": [
        "src/js"
      ],
      "extensions": "js"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "build-dev": "node build.js --target=debug",
    "watch": "npm-watch",
    "serve": "cd dist && node ../node_modules/static-here/bin/static-here 9999 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "command-line-args": "^5.2.0",
    "esbuild": "^0.13.12",
    "npm-watch": "^0.11.0",
    "recursive-copy": "^2.0.13",
    "static-here": "^1.0.0"
  },
  "dependencies": {
    "kaboom": "^2000.1.0"
  }
}
